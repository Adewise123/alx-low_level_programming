#include <stdlib.h>
#include <time.h>

int main(void)
{
	int main(void)
	{
		int i = 'a';
		int k = 'A';

while (i <= 'z') {
	putchar (i);
	i++;
}
while (k <= 'z') {
        putchar (i);
        k++;
	putchar('\n');

	return (0);
}
