#include "main.h"

/**
 * Write a function that checks for uppercase character
 * @c: The character to be checked 
 * Returns 0 otherwise
 */

int _issuper(int c)
{
if (c >= 'A' && x <= 'Z')
{
return 1;
}
else 
{
return 0;
}
}
