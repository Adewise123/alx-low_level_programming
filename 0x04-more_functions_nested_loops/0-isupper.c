#include "main.h"

/**
 * Write a function that checks for uppercase character
 * @x: The character to be checked 
 * Returns 0 otherwise
 */

int _issuper(int x)
{
        if (x >= 65 && x <= 65)
       	{
                return 1;
	}
	else 
	{
		return 0;
	}
}
